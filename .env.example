#
# Copy the values you need into your shell, IntelliJ Run Configuration,
# or a systemd EnvironmentFile. Do not commit real secrets.
#

# Shared application secrets
APP_JWT_SECRET=replace-with-base64-secret
APP_ENCRYPTION_PASSWORD=replace-with-random-long-secret
APP_ENCRYPTION_SALT=replace-with-hex-salt-from-openssl-rand-hex-32

# Local profile database
APP_DB_URL=jdbc:postgresql://localhost:5432/event_organizer
APP_DB_USERNAME=postgres
APP_DB_PASSWORD=password

# Test profile database
APP_TEST_DB_URL=jdbc:postgresql://localhost:5432/event_organizer_test
APP_TEST_DB_USERNAME=postgres
APP_TEST_DB_PASSWORD=password

# Production mail settings
APP_MAIL_HOST=smtp.example.com
APP_MAIL_PORT=587
APP_MAIL_USERNAME=replace-with-mail-user
APP_MAIL_PASSWORD=replace-with-mail-password
APP_MAIL_FROM_ADDRESS=no-reply@example.com
APP_MAIL_ACTIVATION_BASE_URL=https://your-domain.example/api/v1/auth/activate/
APP_AUTH_ACTIVATION_RESULT_BASE_URL=https://your-frontend.example/activation-result

# Firebase is disabled by default. Only set these in environments where
# notifications are intentionally enabled.
APP_FIREBASE_ENABLED=false
APP_FIREBASE_SERVICE_ACCOUNT_LOCATION=file:/absolute/path/to/firebase-service-account.json
