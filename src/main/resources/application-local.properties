spring.datasource.url=${APP_DB_URL}
spring.datasource.username=${APP_DB_USERNAME}
spring.datasource.password=${APP_DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate

app.seed.local-data-enabled=true
app.auth.activation-result-base-url=http://localhost:8080/activation-result
app.firebase.enabled=true
app.firebase.service-account-location=classpath:EventOrganizerFirebaseKey.json
app.notifications.delivery.worker-enabled=false
app.notifications.devices.cleanup-enabled=false
