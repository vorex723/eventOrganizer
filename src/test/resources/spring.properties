# Close least-recently-used Spring contexts during a full integration-test run.
# Each context owns a Hikari pool; retaining Spring's default 32 contexts can
# exhaust the small local PostgreSQL test instance used by IntelliJ.
spring.test.context.cache.maxSize=4
